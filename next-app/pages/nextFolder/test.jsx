//in next like react we can have dropping component that are not page components so
//things like a Navbar or contact component so they can be used in multiple project as a page components

//sfc : stateless functional component
//localhost 3000/nextFolder/test
const Test = () => {
    return ( 
        <div>
            <h1>Test</h1>
        </div>
     );
}
 
export default Test;